@{ Layout = null;}
@model Home.Models.Entity.Usuario

<form role="form" action="#">
    <div class="form-group">
        <label class="control-label">Nome</label>
        <input type="text" placeholder="@Model.Nome" class="form-control" id="nome" @(ViewBag.IsUsuarioLogado ? string.Empty : "readonly") />
    </div>
    <div class="form-group">
        <label class="control-label">Sobrenome</label>
        <input type="text" placeholder="@Model.Sobrenome" class="form-control" id="sobrenome" @(ViewBag.IsUsuarioLogado ? string.Empty : "readonly") />
    </div>
    <div class="form-group">
        <label class="control-label">Apelido</label>
        <input type="text" placeholder="@Model.Apelido" class="form-control" id="apelido" @(ViewBag.IsUsuarioLogado ? string.Empty : "readonly") />
    </div>
    <div class="form-group">
        <label class="control-label">E-mail</label>
        <input type="text" placeholder="@Model.Email" class="form-control" id="email" @(ViewBag.IsUsuarioLogado ? string.Empty : "readonly") />
    </div>
    <div class="form-group">
        <label class="control-label">Data de Nascimento</label>
        <input type="text" placeholder="@Model.NomeDataNascimento" class="form-control date-picker" id="dataNascimento" @(ViewBag.IsUsuarioLogado ? string.Empty : "readonly") />
    </div>
    @if (ViewBag.IsUsuarioLogado)
    {
        <div class="margiv-top-10">
            <a href="javascript:;" onclick="editarPerfil()" class="btn green-haze">Salvar</a>
            <a href="@Url.Action("Index", "Timeline")" class="btn default">Cancelar</a>
        </div>

        <Script>

            function editarPerfil() {
                var model = {
                    Id: @Model.Id,
                    Nome: $("#nome").val() || '@Model.Nome',
                    Sobrenome: $("#sobrenome").val() || '@Model.Sobrenome',
                    Apelido: $("#apelido").val() || '@Model.Apelido',
                    Email: $("#email").val() || '@Model.Email',
                    DataNascimento: $("#dataNascimento").val() || '@Model.DataNascimento'
                }

                $.post("@Url.Action("AtualizaUsuario", "Perfil")", model)
                    .done(function() {
                        notify.success("Alterações realizadas com sucesso!");
                    })
                    .fail(function() {
                        notify.error("Erro ao editar usuario");
                    });
            }
        </Script>
    }
</form>